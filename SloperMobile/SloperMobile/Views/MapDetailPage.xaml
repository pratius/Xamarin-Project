<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:UC="clr-namespace:SloperMobile.UserControls;assembly=SloperMobile"
             x:Class="SloperMobile.Views.MapDetailPage" Style="{StaticResource AppBackgroundColor}" NavigationPage.HasNavigationBar="False">

  <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="40"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="02"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="02"/>
    </Grid.ColumnDefinitions>
    <Image Grid.Row="1" Grid.Column="1" Source="{Binding SectorImage}"  WidthRequest="100" Aspect="AspectFill" HeightRequest="200">
      <Image.GestureRecognizers>
        <TapGestureRecognizer
            Command="{Binding TapSectorCommand}" CommandParameter="RoutePage"/>
      </Image.GestureRecognizers>
    </Image>
    <Grid Grid.Row="2" Grid.Column="1">
      <ListView  x:Name="lstView" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" ItemsSource="{Binding SecData}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="auto"/>
                  <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Text="{Binding TitleText}" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" TextColor="White"/>
                <Label Grid.Row="1" Grid.ColumnSpan="2" TextColor="White" LineBreakMode="WordWrap" Text="{Binding SubText}"  HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand"/>
                <!--<Image Source="{Binding ImagePath}" Aspect="AspectFit" Grid.Column="1" HorizontalOptions="EndAndExpand" VerticalOptions="CenterAndExpand"/>-->
              </Grid>

            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </Grid>
    
    <UC:HeaderUC Grid.ColumnSpan="3" HeightRequest="30"/>
    <UC:FooterUC  Grid.Column="1" Grid.Row="3" BackgroundColor="#1A1A1A" VerticalOptions="EndAndExpand" HorizontalOptions="FillAndExpand"/>
  </Grid>

</ContentPage>